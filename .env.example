# Optional build-time search verification values. Leave blank to emit no tag.
GOOGLE_SITE_VERIFICATION=
BING_SITE_VERIFICATION=

# Optional IndexNow key used only by scripts/submit-indexnow.mjs.
# When configured, create public/<key>.txt containing only the same key.
INDEXNOW_KEY=
